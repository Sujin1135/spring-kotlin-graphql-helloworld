type Query {
    students: [Student]
    startupLogs: [StartupLog]
}

type Student {
    id: String,
    city: String,
    fname: String,
}

type StartupLog {
    id: String,
    hostname: String,
    pid: String,
}
